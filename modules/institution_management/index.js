/**
 * Institution Management Module
 * Handles institution creation, updates, and retrieval
 */

const institutionRoutes = require('./routes/institutionRoutes');

module.exports = {
    routes: institutionRoutes
};
